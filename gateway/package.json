{
  "name": "guardian-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "SafeRaipur Guardian Mode SIM gateway — a spare Android phone that physically places the calls and SMS queued by the Postgres escalation engine.",
  "scripts": {
    "android:add": "cap add android",
    "android:sync": "cap sync android",
    "android:open": "cap open android",
    "dev": "npx http-server www -p 8080 -c-1"
  },
  "devDependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/cli": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "http-server": "^14.1.1"
  }
}
